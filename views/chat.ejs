<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Turps | Chat</title>
  <link rel="stylesheet" href="css/style.css" media="all">
</head>
<body>
  <main>
    <h1>Turps | Chat</h1>
    <div class="messages">
      <% for (let i = 0; i < messages.length; i++) { %>
        <div class="message">
          <p><%= messages[i].usernameOfUser %></p> 
          <p> - <%= messages[i].messageOfUser %></p>
        </div>
        
      <% } %>
    </div>
    <form action="/chat" method="POST">
      <input type="text" name="message" autocomplete="off" placeholder="Type your message here..." required />
      <br />
      <button type="submit" name="send" value="<%= username %>">Send</button>
    </form>
      <a href="/logout"><button>Logout</button></a>
  </main>
</body>
</html>
